import React from "react";
import styles from "../../css/searchCenter/SearchCenterModal.module.scss";

type Props = {};

const SearchCenterModal = ({}: Props) => {
	return (
		<aside className={styles.SearchCenterModal}>
			{/*  */}
			{/*  */}
		</aside>
	);
};

export default SearchCenterModal;

SearchCenterModal.defaultProps = {};

SearchCenterModal.propTypes = {};
